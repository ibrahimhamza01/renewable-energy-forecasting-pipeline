airflow:
  enabled: false
  dag_id: wind_pipeline_dag
  schedule_interval: "@daily"
  catchup: false

paths:
  dags_dir: "infra/airflow/dags"
  plugins_dir: "infra/airflow/plugins"

runtime:
  retries: 1
  retry_delay_minutes: 5
