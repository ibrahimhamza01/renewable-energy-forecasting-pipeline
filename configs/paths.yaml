# Logical path definitions
# These are NOT real paths — they are abstract names
# They are resolved through user config + runtime config

data:
  raw_isd: ""

bronze:
  isd: "bronze/isd"

silver:
  weather: "silver/weather"

gold:
  wind_station_hourly: "gold/wind/station/hourly"
  wind_station_daily: "gold/wind/station/daily"
  wind_region_daily: "gold/wind/region/daily"
  wind_region_monthly: "gold/wind/region/monthly"

  wind_ml_base: "gold/wind/ml/base"
  wind_ml_features: "gold/wind/ml/features"
  wind_ml_train: "gold/wind/ml/train"
  wind_ml_validation: "gold/wind/ml/validation"
  wind_ml_test: "gold/wind/ml/test"

models:
  registry: "models/registry"

forecasts:
  outputs: "forecasts/outputs"

benchmarks:
  results: "benchmarks/results"

outputs:
  figures: "figures"
  metrics: "metrics"
  sample_runs: "sample_runs"
  parsed_sample: "sample_runs/parsed_sample"
  station_master_contiguous_us: "sample_runs/station_master_contiguous_us.csv"
  cleaned_enriched_sample: "sample_runs/cleaned_enriched_sample"